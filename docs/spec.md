# 輸入方案規格 — 阿特雙拼輸入法

本檔為輸入行為的**規格權威**；`core\` 的實作與測試以此為準。
鍵位源自 Rime 方案 `mspy_zhuyin.schema.yaml`（speller/algebra）的定案，移植時以本檔表格為可讀形式。

## 1. 音節結構

每個音節 = **固定 2 鍵**（聲母鍵 + 韻母鍵）+ **選擇性聲調數字**（1-5）。
音節邊界是決定性的：第 2 鍵落下即音節完成；其後若是數字則為聲調，否則開始下一音節。

## 2. 聲母鍵

| 聲母 | 鍵 | 備註 |
|---|---|---|
| b p m f d t n l g k h j q x r z c s | 同字母 | |
| zh | `v` | |
| ch | `i` | |
| sh | `u` | |
| y w | 同字母 | 拼音 yi/wu/yu 系列視為 y/w 聲母 |
| （零聲母） | `o` | 見 §4 |

## 3. 韻母鍵

| 韻母 | 鍵 | 例 |
|---|---|---|
| a | `a` | ba=`ba` |
| o | `o` | bo=`bo` |
| e | `e` | he=`he` |
| i | `i` | bi=`bi` |
| u | `u` | bu=`bu` |
| ü | `y` | nü=`ny`、lü=`ly` |
| ai | `l` | shai=`ul` |
| ei | `z` | bei=`bz` |
| ao | `k` | bao=`bk` |
| ou | `b` | dou=`db` |
| an | `j` | ban=`bj` |
| en | `f` | ben=`bf` |
| ang | `h` | bang=`bh` |
| eng | `g` | beng=`bg` |
| er | `r` | er=`er`/`or` |
| ia | `w` | jia=`jw` |
| ie | `x` | jie=`jx` |
| iao | `c` | jiao=`jc` |
| iu | `q` | jiu=`jq` |
| ian | `m` | jian=`jm` |
| in | `n` | jin=`jn` |
| iang | `d` | jiang=`jd` |
| ing | `;` | xing=`x;` |
| iong | `s` | xiong=`xs` |
| ua | `w` | hua=`hw` |
| uo | `o` | duo=`do` |
| uai | `y` | shuai=`uy` |
| ui | `v` | shui=`uv` |
| uan | `r` | duan=`dr` |
| un | `p` | dun=`dp` |
| uang | `d` | shuang=`ud` |
| ong | `s` | zhong=`vs` |
| üe | `t`（亦可 `v`） | jue=`jt`/`jv` |
| üan | `r` | juan=`jr` |
| ün | `p` | jun=`jp` |

## 4. 零聲母音節

- 通則：`o` + 韻母鍵。例：安=`oj`、昂=`oh`、歐=`ob`、啊=`oa`、餓=`oe`。
- a/e 開頭音節另可用**首字母雙打形**：啊=`aa`、愛=`al`、安=`aj`、恩=`ef`、餓=`ee`、二=`er`。
- 兩種拼法並存（與 Rime 版一致）。

## 5. 聲調語意（嚴格模式，2026-07-26 拍板，勿再反覆）

| 輸入 | 候選範圍 |
|---|---|
| 不打數字 | **只出一聲＋輕聲**字詞（`de` → 得(ㄉㄜ)、的(ㄉㄜ˙)；不出 ㄉㄜˊ） |
| `1` | **嚴格只出一聲**（`de1` 不出「的」） |
| `2` `3` `4` | 精確該聲調 |
| `5` | 只出輕聲 |

- 「我」必須打 `wo3`；`wo` 只出「窝」等一聲字。
- **聲調鍵左右手鏡像（2026-08-04 新增）**：右手以 5／6 之間為軸取得同一組聲調——

  | 聲調 | 左手 | 右手 |
  |---|---|---|
  | 一聲 | `1` | `0` |
  | 二聲 | `2` | `9` |
  | 三聲 | `3` | `8` |
  | 四聲 | `4` | `7` |
  | 輕聲 | `5` | `6` |

  兩組完全等價（`ge1` ＝ `ge0` ＝ ㄍㄜˉ → 歌）。鏡像鍵與 `8` 開選單／`9`/`0` 移游標的衝突，
  以「未定案窗口」切開：見 §6〈數字鍵的兩種身分〉。
- **該聲調查無條目時＝按鍵吃掉、聲調窗口不關**（`vs2`：詞庫無 ㄓㄨㄥˊ，維持 ㄓㄨㄥ，可再按 `3`）。
- 詞庫鍵格式：一聲不標記、2/3/4 聲尾綴 `ˊˇˋ`、輕聲尾綴 `˙`。
  無數字查詢＝「無標記 ∪ `˙` 尾綴」合併；明打 `1` 只查無標記鍵（實作經內部哨兵記號區分）。
  組字畫面上**明打的一聲標 `ˉ`（U+02C9）**，與「還沒給聲調」的裸注音區分；輕聲畫面上一律**尾綴** `˙`
  （2026-08-04 拍板：與 2/3/4 對齊，不採注音書寫的前綴慣例）。
- 決策記錄（2026-07-27 拍板不採用）：曾評估「含輕聲的詞加去聲調 alias 鍵」讓 `erzi` 免打聲調出「兒子」；確認為使用者操作誤解（打實調 `er2zi` 即可），嚴格語意維持原樣。

## 6. 模態互動（微軟注音式）

### Composing（組字中）
- 行內顯示：已轉換的整句最佳路徑 + 未完成音節的注音尾（如 `ㄕ`）。
- **首鍵一律顯示注音、不顯示英文字母**（2026-07-27 對齊）：有聲母符號的鍵照聲母（b→ㄅ、u→ㄕ…）；無聲母符號的鍵顯示代表性符號——`y`→ㄧ、`w`→ㄨ、`a`→ㄚ、`e`→ㄜ、`o`→ㄛ（第二鍵落下後修正為實際音節）。
- **單色顯示（2026-07-29 定案，取代雙色）**：整段組字＝應用程式預設文字色＋**虛線底線直到 commit**。決策記錄：原「藍色＝聲調未定/殘鍵」的換色只有完整 TSF app（記事本/Word）會畫、LINE/Sublime 等一律不顯示，應用面太窄故整個移除；黑/藍分段語意仍存在於內部 segments（游標與錨點計算沿用），僅視覺取消。選字錨點淡藍反白**保留**。
  - 顯示屬性實作註記（2026-07-28）：定調段的 attr-info 用 `TF_ATTR_INPUT`——Chromium 系 app（LINE、Chrome）會把 `TF_ATTR_TARGET_CONVERTED` 畫成黑色選取反白並忽略顏色；選字錨點淡藍反白維持 `TF_ATTR_TARGET_CONVERTED`（在 Chromium 呈反白屬可接受的強調）。
- 每完成一個音節即重跑整句轉換（walk），前文可能隨上下文自動修正。
- **未定案窗口（2026-08-04 擴大，取代 2026-08-02 版）**：音節第 2 鍵落下後**不顯示轉換後的字**，維持注音（`hk` → ㄏㄠ，不是「蒿」）；**打了聲調也一樣維持注音**（`zv7` → ㄗㄨㄟˋ，不是「最」）。該音節其實已進詞格（整句轉換照跑、前文照樣自動修正），只是顯示層蓋成注音＋調號。
  - **定案**（注音轉成文字）的觸發：**空白鍵、標點符號、下一音節首鍵、`` ` ``**。
  - 同時只有**最新**一個音節處於未定案。
- **組字中數字全為控制鍵，絕不字面輸出**（2026-07-27 大改版拍板）。
  **數字鍵的兩種身分（2026-08-04 拍板）**——由「有沒有未定案的音節」決定，畫面上看得出來（有注音＝未定案）：

  | 狀態 | `0`-`9` | `-`/`=` |
  |---|---|---|
  | 未定案、**還沒給聲調**（ㄏㄠ） | **全部是聲調鍵**（含鏡像，見 §5） | 吃掉無作用 |
  | 未定案、**已給聲調**（ㄏㄠˇ） | **全部吃掉無作用** | 吃掉無作用 |
  | 已定案（好） | `8` 開選單、`9`/`0` 移游標、`1`-`7` 吃掉 | 游標跳組字串最前／最後 |

  - 拍板理由：規則單一——「未定案 → 數字不做控制」。代價是句尾要選字得先按一次空白定案（`ni3hk3` → 空白 → `8`）；已評估「給調後控制鍵立刻回歸」的替代案，使用者選擇規則一致優先。
  - `-`/`=` 一併納入禁用（2026-08-04），否則會出現「`-` 能移游標、`9` 不能」的不一致。閒置時 `-`/`=` 仍為 Home/End 導航鍵，見〈閒置導航鍵〉。
  - **第二個聲調鍵吃掉無作用**（不改調）；打錯調用 **Backspace 退調**：ㄏㄠˇ 按 Backspace → 回到 ㄏㄠ（未給調），可重打聲調；再按一次才刪掉整個音節。查無無聲調讀音的音節（ㄗㄨㄟ）退調後回到「等待聲調」的待定狀態，行為相同。
- **無組字時按數字：吃掉無作用（2026-07-27 拍板：中文模式數字排整個禁用）**。要輸入數字＝先 Shift 切英文（分隔空白由 Shift 機制自動處理），養成「打數字先切英文」的習慣。
- **九宮格（NumPad）豁免（2026-07-28 新增）**：禁用只針對字母上方的數字排。NumPad 數字與運算符／小數點（`* + - . /`）閒置時直接放行字面輸入；**組字中按 NumPad＝先整段自動上屏、再輸出該字元**（比照標點 commit 慣例）。NumLock 關閉時 NumPad 送方向鍵等，維持既有行為。
- **游標錨點反白**：游標右邊的那個字以淡藍底色強調，表示按 `8` 會從該字開始選字；游標在最右端時無反白，此時按 `8` 選最後一個字。
- **方向鍵（←/→/↑/↓/PgUp/PgDn）在組字中一律吃掉無作用**（2026-07-27 移除選字功能；避免游標逃出組字範圍）。
- **空白鍵＝定案，不 commit（2026-08-02 改版，取代「與 Enter 同」）**：
  - 有待定注音／音節時：**只定案不上屏**——完整音節以「一聲/輕聲」預設讀音轉成字（ㄏㄠ → 蒿）；沒有無聲調讀音的注音（半個音節 `n`、或詞庫查無無聲調條目的 ㄋㄧㄠ）則把注音本身定案成固定符號融入組字串（與 `` ` `` 同一機制）。
  - **沒有東西可定案時**（最新音節已定案，或剛打完標點）：整段 commit，與 Enter 同。
  - 閒置時：一般半形空白（放行給應用程式）。
- **標點符號＝定案並融入組字串，不 commit（2026-08-04 改版，取代「連同前文一起上屏」）**：
  - 打標點時先把未定案音節定案成字（`zv7` ㄗㄨㄟˋ → 按 `,` → 「最，」），標點本身以固定符號（literal 讀音）加進組字串，**整段維持虛線底線、未上屏**。
  - 標點之後**照樣可以選字**：游標走得過標點，`8` 對標點兩側的字開選單；Backspace 逐符號刪。
  - **閒置時打標點＝開一段新的未 commit 組字串**（而非直接上屏）。
  - **不設任何自動上屏界線（2026-08-04 拍板）**：組字串可以無限長，只有 **Enter／空白（無可定案時）／Shift 切英文／失焦** 會上屏。已評估「句末標點仍上屏」「字數上限」兩種安全網，使用者選擇完全手動控制。
  - 表：`,` `.` `?` `!` `:` `\` `[` `]` `{` `}` `(` `)` `<` `>` `^` `_` `~` → 全形「，。？！：、「」『』（）《》……——～」；`"`/`'` 開閉交替；`;` 單獨時為「；」、組字中仍為 ing 韻母鍵。
- Backspace：音節內逐鍵刪；已給聲調的未定案音節先**退調**（見〈數字鍵的兩種身分〉）；其餘逐音節／逐符號刪（游標前）。
- Enter：整句上屏（未定案的殘鍵——半個音節等——仍丟棄）。
- Esc：清空組字（候選選單開啟時一併取消）。**一按全清，不分段**（2026-08-04 拍板：標點不再上屏後組字串可能很長，仍評估後維持原行為，不加「先取消未定案再清空」的兩段式）。

### Selecting（候選選單開啟）
- 進入：**僅**數字 `8`（於游標錨點的詞段開窗；游標在最右端時選最後一個字）。
- 每頁最多 **6** 個候選；數字 `1`-`6` 選字（選定即釘選該詞段，關窗回 Composing）；按了本頁沒有的編號＝無作用。
- **選定後游標跳到該詞段之後（2026-08-02 新增）**：錨點自動變成下一個字，可以連續按 `8` 一路往右改完整句，不必再用 `9`/`0` 移游標；選的是多字詞就整段跳過（選 2 字詞＝游標前進 2）。游標已在最右端時留在原地（`8` 仍對最後一個字開窗）。
- **無效候選過濾（2026-07-27）**：候選值與其跨距目前顯示的文字相同者（選了也不會變）一律隱藏——「聽不懂」錨在懂上時不再列出 聽不懂/不懂/懂 自己；過濾後若一個候選都不剩，選單不開啟。
- 翻頁：`8` 下一頁、`7` 上一頁；**不環繞**（邊界按了無作用）。
- **其他任何按鍵＝關窗並執行該鍵原本功能**：`9`/`0` 關窗並移動游標、字母關窗並在游標處輸入、Backspace 關窗並刪字、Enter 關窗並 commit、Esc 關窗並清空。
- 候選窗 UI：白底圓角卡片、細邊框、選中列淡藍強調、灰色數字標籤、右下角頁碼（如 `2/4`）。
- **候選窗依螢幕縮放比例放大（2026-08-02 修正）**：候選窗是自繪 GDI 視窗，所有尺寸（字級 10pt、列高 34、頁碼列 20、邊框 1）以 96 dpi 為基準，實際依 `GetDpiForWindow` 取得所在螢幕的 DPI 換算，字型也依該 DPI 重建（跨螢幕拖曳走 `WM_DPICHANGED` 重算）。原本吃 `Global::defaultlFontHandle`（開機時以行程系統 DPI 建一次）——per-monitor aware 的宿主（LINE、Chrome）回報 96，在 150%／175% 螢幕上就會變得特別小。
- 候選窗**開在游標錨點字的正下方**（比照微軟注音；layout 追蹤錨點那一個字的子範圍，非整段組字起點）；**下方放不下（貼近螢幕/工作區底部）時自動翻到錨點字上方**，左右超界時貼邊。

### 閒置導航鍵（2026-07-28 新增）

無組字（idle）時，以下按鍵**吃掉並代送**對應導航鍵（SendInput 注入；實體按住 Shift 時注入鍵自然成為選取延伸）：

| 按鍵 | 代送 | 按住 Shift |
|---|---|---|
| `9` | ← | （不攔：Shift+9 仍出「（」） |
| `0` | → | （不攔：Shift+0 仍出「）」） |
| `-` | Home（行首） | 選取到行首 |
| `=` | End（行尾） | 選取到行尾 |

- 決策記錄（2026-07-28 拍板）：`-`/`=` 不分 Shift 一律攔下——中文模式閒置時從此打不出 `-`/`=`/`_`（含 `_`→「——」的閒置對映）與 `+`；要打就 Shift 切英文（與數字禁用同一邏輯）。組字中 Shift+`-`＝「——」不受影響。Shift+9/0 保留（）故字元級選取不提供，選取只有行首／行尾兩種。
- 注入鍵重置 Shift 單獨輕按偵測，故按住 Shift 導航後放開 Shift 不會誤觸中英切換。
- 決策記錄（2026-07-28 撤銷）：曾實作「閒置按 `8` 對既有文字反查選字（重選字）」，實測大多數應用程式不支援在已上屏文字上讀取／開 composition（僅文字編輯器可用），整組功能已移除；閒置 `8` 回歸吃掉無作用。

### 注音符號輸入（2026-07-27 v3 定案：`` ` `` 定案融入組字串）
- **`` ` ``（反引號）＝把注音「定案」成固定黑字**，融入組字串、維持未 commit（空白鍵對「查無無聲調讀音」的注音走同一條路）：
  - 有待定注音時（藍色殘鍵）：`n` 顯示藍ㄋ → 按 `` ` `` → ㄋ 轉黑定案（`ul``` ` ```→定案ㄕㄞ）。
  - 無待定注音時：`` ` `` 挖空聲母，下一鍵讀為「韻母鍵」、其注音**直接定案**（`` `k ``→ㄠ、`` `x ``→ㄧㄝ；歧義鍵取主韻母，完整表見 `HollowFinalDisplay`）。
- 定案的注音是詞格中的單一候選 literal 節點（`\x01`+符號 讀音）：可與中文混排、游標編輯、Backspace 逐符號刪，Enter/Space 與整段一起上屏。ㄋㄧㄠ＝`` n`y``k ``一次組完。
- **Space 對注音殘鍵＝定案（2026-08-02 起不再上屏）**：`n`+Space→ㄋ 留在組字串裡，要上屏再按 Enter／空白；**Enter 仍丟棄未定案的殘鍵**。
- 對定案注音開選單只有它自己一個候選→經「無效候選過濾」後選單不開啟。

### 中英切換（Shift 輕按，2026-07-27 v2 定案：commit 式）
- **單獨輕按 Shift**（按下到放開之間無其他鍵）＝中/英模式切換；Shift+字母照常輸出大寫、不觸發切換。實作走 TSF **preserved key**（樣本原本的 IME-mode 保留鍵接管改造）——TSF 會在 KeyEventSink 之前攔截，不能用按鍵事件偵測。
- 英文模式＝**系統鍵盤關閉狀態**（GUID_COMPARTMENT_KEYBOARD_OPENCLOSE）：工具列中/英指示跟著變（與微軟注音一致）、按鍵全數放行、英文為一般無底線編輯。
- **中英模式各應用程式獨立記憶（2026-08-02 新增，比照微軟注音／Rime）**：
  - 每個**應用程式（行程）**記自己的中/英；剛啟動的應用程式一律**英文**（`InitializeSampleIMECompartment` 設 FALSE）。
  - 焦點回到某應用程式時（`ITfThreadMgrEventSink::OnSetFocus` 拿到非空 doc mgr、`ITfKeyEventSink::OnSetFocus(TRUE)`）把記憶中的模式寫回 compartment——TIP 本來就跑在該應用程式的行程內，這份記憶天然是 per-app；重寫是為了蓋掉系統跨應用程式同步的開關狀態。
  - 記憶只由**自己看得到的切換**更新（Shift 輕按）。工具列點擊 中/英 圖示切換無法與系統同步寫入區分，故不納入記憶（切換到別的 app 再回來會回到記憶值）。
- **分隔空白規則（2026-07-29 v4：純被動記憶，取代 v3 讀文件）**：
  - 決策記錄：v3「讀游標左邊字元」只有完整 TSF app（記事本/Word）讀得到，Chromium 系（LINE/Chrome）的 TSF text store 只暴露組字段、Sublime 走 IMM32、Terminal 為最小化實作——大多數 app 一律讀不到。故**完全不讀文件**，改用輸入法自己能知道的資訊，行為單一可控。
  - 記憶來源：**自己 commit 的最後一個字**＋**英文模式（鍵盤關閉）下目擊放行給 app 的按鍵**（字母/數字→英文類、空白/標點→其他類）。
  - 切**英文**時：記憶是**中文字** → 補一個半形空白；組字中按 Shift 則先 commit、以 commit 尾字判斷（不依賴記憶）。
  - 切**中文**時：記憶是**英文字母或數字** → 補一個半形空白。
  - **保守歸零（寧缺勿濫）**：焦點切換、方向鍵/Home/End/PgUpDn、Enter/Tab/Esc/Backspace/Delete、Ctrl/Alt 組合鍵（貼上等）、**任何滑鼠按下**（執行緒內 WH_MOUSE hook——LINE/Sublime 等宿主不會把游標移動回報給 TSF，直接在宿主行程內看滑鼠事件；不跨行程）、app 回報的**純游標移動**（selection 變更且同一編輯記錄內無文字變更；打字與自身 commit 是文字＋selection 一起變、不歸零）、組字被點擊他處終止——以上任一發生即歸零為「未知」，未知一律不補空白。
  - **切英文補空白前的最後防線**：commit 當下存游標位置快照（ITfRange clone），補空白前比對游標仍在該位置才補——中文記憶只源自自己的 commit、其間無打字、文字未變，故此比對在**不回報游標事件的 app** 也成立。比不出來（快照缺、跨 context、非空選取）一律不補。錯誤模式＝偶爾少一個空白，不會多。
- 底線內英文段機制（v1）已整個移除；composer 現在只管中文。

## 7. 學習

- 手動選字 → UserOverrideModel `observe`（LRU + 時間衰減）；每次插入後 `suggest` 重排。
- 上屏 → 使用者詞持久化（`%APPDATA%\MspyIME\user-phrases.txt`，`值 讀音鍵` 格式，附加後重載）。

## 8. 規格測試例（M2 測試套件的最低集合）

| 按鍵序列 | 期望 |
|---|---|
| `ul4` | 顯示注音 ㄕㄞˋ；空白定案「曬」 |
| `vs` | 顯示注音 ㄓㄨㄥ；再按空白＝定案「中」（一聲） |
| `oj3` | 顯示注音 ㄢˇ；空白定案「俺」 |
| `x;` | 顯示注音 ㄒㄧㄥ；空白定案「星」 |
| `zv7` | 顯示注音 ㄗㄨㄟˋ（右手四聲）；空白定案「最」 |
| `ge1` / `ge0` | 皆顯示 ㄍㄜˉ；定案「歌」（明打一聲，非「個」） |
| `ge5` / `ge6` | 皆顯示 ㄍㄜ˙；定案「個」 |
| `vs3` + `3` | 第二個聲調鍵吃掉，維持 ㄓㄨㄥˇ |
| `vs3` + Backspace | 退調回 ㄓㄨㄥ；再 Backspace 整個音節消失 |
| `ul4` + Backspace | 退調回 ㄕㄞ（無無聲調讀音，回到等待聲調） |
| `vs2` | 詞庫無 ㄓㄨㄥˊ：吃掉，維持 ㄓㄨㄥ，可再按 `3` |
| `ni3hk3` + `8`/`9`/`0`/`-`/`=` | 全部吃掉無作用（未定案）；空白定案後才生效 |
| `hk` + 空白 + 空白 | ㄏㄠ → 蒿（定案，未上屏）→ 上屏「蒿」 |
| `n` + 空白 | 「ㄋ」定案進組字串（**不**上屏） |
| `nc` + 空白 | 「ㄋㄧㄠ」定案為注音符號（無無聲調讀音） |
| `de` | 候選含 的(ㄉㄜ˙)、得(ㄉㄜ)；不含 ㄉㄜˊ 字 |
| `de1` | 候選不含 的 |
| `de5` | 候選只有輕聲字 |
| `wo3` | 候選含 我 |
| `wo` | 候選為一聲字（窝…），不含 我 |
| 空組字時 `2` | 吃掉無作用（數字需切英文模式） |
| `vs` + 空白 + `8` | 開候選選單；`1`-`6` 選字後關窗 |
| `ni3hk3wo3` + 空白 + `-` + `8` + 選字 | 選定後游標右移一格，錨點變「好」（可連按 `8` 續改） |
| 切中文後跳到另一個 app | 該 app 維持自己的模式（沒切過＝英文） |
| `ni3hk3` + 空白 + `9` `9` | 游標移到最前，錨點反白「你」 |
| `ni3hk3` + 空白 + `0` | 最右端環繞到最左 |
| `ni3hk3` + 空白 + `-` | 游標跳到最前，錨點反白「你」；再 `=` 跳回最後 |
| 組字中 Shift 輕按 | 整段 commit＋補「␣」、切英文（工具列顯示英） |
| 英文打 `ok` 後 Shift 輕按 | 上屏「␣」、切回中文 |
| 剛切視窗就 Shift 輕按 | 純切換，不加空白 |
| 空白（閒置） | 字面空白 |
| 空白（音節已定案） | 整段 commit（與 Enter 同） |
| `,` `.`（閒置） | 開新組字串「，」「。」，**不上屏** |
| `ni3hk3` + `,` + `zv7` + `.` | 組字串「你好，最。」整段未上屏；Enter 才 commit |
| `ni3hk3` + `,` + `9` `9` + `8` | 游標走得過標點，對「好」開選單 |
