# 輸入方案規格 — Wei雙音拚輸入法

本檔為輸入行為的**規格權威**；`core\` 的實作與測試以此為準。
鍵位源自 Rime 方案 `mspy_zhuyin.schema.yaml`（speller/algebra）的定案，移植時以本檔表格為可讀形式。

## 1. 音節結構

每個音節 = **固定 2 鍵**（聲母鍵 + 韻母鍵）+ **選擇性聲調數字**（1-5）。
音節邊界是決定性的：第 2 鍵落下即音節完成；其後若是數字則為聲調，否則開始下一音節。

## 2. 聲母鍵

| 聲母 | 鍵 | 備註 |
|---|---|---|
| b p m f d t n l g k h j q x r z c s | 同字母 | |
| zh | `v` | |
| ch | `i` | |
| sh | `u` | |
| y w | 同字母 | 拼音 yi/wu/yu 系列視為 y/w 聲母 |
| （零聲母） | `o` | 見 §4 |

## 3. 韻母鍵

| 韻母 | 鍵 | 例 |
|---|---|---|
| a | `a` | ba=`ba` |
| o | `o` | bo=`bo` |
| e | `e` | he=`he` |
| i | `i` | bi=`bi` |
| u | `u` | bu=`bu` |
| ü | `y` | nü=`ny`、lü=`ly` |
| ai | `l` | shai=`ul` |
| ei | `z` | bei=`bz` |
| ao | `k` | bao=`bk` |
| ou | `b` | dou=`db` |
| an | `j` | ban=`bj` |
| en | `f` | ben=`bf` |
| ang | `h` | bang=`bh` |
| eng | `g` | beng=`bg` |
| er | `r` | er=`er`/`or` |
| ia | `w` | jia=`jw` |
| ie | `x` | jie=`jx` |
| iao | `c` | jiao=`jc` |
| iu | `q` | jiu=`jq` |
| ian | `m` | jian=`jm` |
| in | `n` | jin=`jn` |
| iang | `d` | jiang=`jd` |
| ing | `;` | xing=`x;` |
| iong | `s` | xiong=`xs` |
| ua | `w` | hua=`hw` |
| uo | `o` | duo=`do` |
| uai | `y` | shuai=`uy` |
| ui | `v` | shui=`uv` |
| uan | `r` | duan=`dr` |
| un | `p` | dun=`dp` |
| uang | `d` | shuang=`ud` |
| ong | `s` | zhong=`vs` |
| üe | `t`（亦可 `v`） | jue=`jt`/`jv` |
| üan | `r` | juan=`jr` |
| ün | `p` | jun=`jp` |

## 4. 零聲母音節

- 通則：`o` + 韻母鍵。例：安=`oj`、昂=`oh`、歐=`ob`、啊=`oa`、餓=`oe`。
- a/e 開頭音節另可用**首字母雙打形**：啊=`aa`、愛=`al`、安=`aj`、恩=`ef`、餓=`ee`、二=`er`。
- 兩種拼法並存（與 Rime 版一致）。

## 5. 聲調語意（嚴格模式，2026-07-26 拍板，勿再反覆）

| 輸入 | 候選範圍 |
|---|---|
| 不打數字 | **只出一聲＋輕聲**字詞（`de` → 得(ㄉㄜ)、的(ㄉㄜ˙)；不出 ㄉㄜˊ） |
| `1` | **嚴格只出一聲**（`de1` 不出「的」） |
| `2` `3` `4` | 精確該聲調 |
| `5` | 只出輕聲 |

- 「我」必須打 `wo3`；`wo` 只出「窝」等一聲字。
- 詞庫鍵格式：一聲不標記、2/3/4 聲尾綴 `ˊˇˋ`、輕聲尾綴 `˙`。
  無數字查詢＝「無標記 ∪ `˙` 尾綴」合併；明打 `1` 只查無標記鍵（實作經內部哨兵記號區分）。

## 6. 模態互動（微軟注音式）

### Composing（組字中）
- 行內顯示：已轉換的整句最佳路徑 + 未完成音節的注音尾（如 `ㄕ`）。
- 每完成一個音節即重跑整句轉換（walk），前文可能隨上下文自動修正。
- 數字 1-5：聲調鍵（作用於剛完成的音節）。
- 待定音節（已完成 2 鍵、無聲調）在下一音節首鍵落下時自動定案為「一聲/輕聲」狀態。
- **無待定音節時按數字**（如直接打「2026」）：數字為字面輸出。
- 空白鍵：**輸出空白字元**（比照微軟注音）。
- `,` / `.`：直接上屏「，」「。」（沿用使用者 Rime 習慣）。
- Backspace：音節內逐鍵刪；音節完成後逐音節刪。
- Enter：整句上屏。
- Esc：清空組字。

### Selecting（候選窗開啟）
- 進入：**僅** ↓ 鍵（於游標所在詞段開窗）。
- 數字 1-9：選字（選定即釘選該詞段，關窗回 Composing）。
- ↑ / Esc：關窗回 Composing。
- 翻頁：PageUp/PageDown（沿用外殼既有機制）。

## 7. 學習

- 手動選字 → UserOverrideModel `observe`（LRU + 時間衰減）；每次插入後 `suggest` 重排。
- 上屏 → 使用者詞持久化（`%APPDATA%\MspyIME\user-phrases.txt`，`值 讀音鍵` 格式，附加後重載）。

## 8. 規格測試例（M2 測試套件的最低集合）

| 按鍵序列 | 期望 |
|---|---|
| `ul4` | 音節 ㄕㄞˋ（曬） |
| `vs` | 音節 ㄓㄨㄥ（一聲，中） |
| `oj3` | 音節 ㄢˇ（俺） |
| `x;` | 音節 ㄒㄧㄥ（一聲） |
| `de` | 候選含 的(ㄉㄜ˙)、得(ㄉㄜ)；不含 ㄉㄜˊ 字 |
| `de1` | 候選不含 的 |
| `de5` | 候選只有輕聲字 |
| `wo3` | 候選含 我 |
| `wo` | 候選為一聲字（窝…），不含 我 |
| 空組字時 `2` | 字面輸出 "2" |
| `vs` + ↓ | 開候選窗；`1`-`9` 選字後關窗 |
| 空白 | 字面空白 |
| `,` `.` | 「，」「。」 |
